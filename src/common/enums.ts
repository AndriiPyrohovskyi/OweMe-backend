export enum UserRole {
    User = "User",
    Premium = "Premium",
    Admin = "Admin"
}

export enum GroupsUserRole {
    Member = "Member",
    Admin = "Admin",
    Owner = "Owner"
}

export enum RequestStatus {
    Opened = "Opened",
    Canceled = "Canceled",
    Accepted = "Accepted",
    Declined = "Declined",
    Closed = "Closed"
}